<div class="row">
  <div class="orbit" role="region" aria-label="Favorite Space Pictures" data-orbit>
    <div class="orbit-wrapper">
      <div class="orbit-controls">
        <button class="orbit-previous"><span class="show-for-sr">Previous Slide</span>&#9664;&#xFE0E;</button>
        <button class="orbit-next"><span class="show-for-sr">Next Slide</span>&#9654;&#xFE0E;</button>
      </div>
      <ul class="orbit-container">
      {% for group in events %}
              <li class="is-active orbit-slide">
                  {% set end = group|length %}
                  {% for key, event in group %}
                      <div class="large-3 medium-3 columns {{ key == end - 1 ? 'end':''}}">
                          <figure class="orbit-figure">
                              <div class="columns large-12 medium-12 wrapper">
                                <span class="date">
                                  {{ event.start.local | date('U') | format_date('colfuturo_d_m_y_') }}
                                </span>
                                <br>
                                <span class="time">
                                  {{ event.start.local | date('h:i A') }} - {{ event.end.local | date('h:i A') }}
                                </span>
                                <br>
                                <h3>
                                  {{ event.name.text }}
                                </h3>
                                <br>
                                {# <img class="img" src="{{ event.logo.url }}"/> #}
                              </div>
                              <div class="large-12 medium-12 button">        
                                  <a href="{{ event.url }}">registrese al evento </a>
                              </div>        
                            
                          </figure>
                      </div>
                  {% endfor %}
              </li>
      {% endfor %}
      </ul>        
    </div>
    <nav class="orbit-bullets">
      <button class="is-active" data-slide="0"><span class="show-for-sr">First slide details.</span><span class="show-for-sr">Current Slide</span></button>
      <button data-slide="1"><span class="show-for-sr">Second slide details.</span></button>
    </nav>
  </div>
</div>